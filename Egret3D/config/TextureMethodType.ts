module egret3d {
  
    /**
    * @private
    */
    export enum TextureMethodType {
        diffuse,
        normal,
        specular,
        color,
        shadow
    }

    export enum MethodType {
        local_vertex,
        global_vertex,
        diffuse,
        specular,
        normal,
        color,
        shadow
    }
}